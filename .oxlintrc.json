{
  "$schema": "./node_modules/oxlint/configuration_schema.json",
  "plugins": ["import", "typescript", "unicorn"],
  "categories": {
    "correctness": "error"
  },
  "options": {
    "typeAware": true
  },
  "env": {
    "builtin": true
  },
  "ignorePatterns": [
    "**/build/**",
    "**/coverage/**",
    "**/dist/**",
    "**/snap/**",
    "**/vite.config.*.timestamp-*.*",
    "graphify-out/**",
    "repos/**",
    "apps/code-art/archive/**"
  ],
  "rules": {
    "no-restricted-imports": [
      "error",
      {
        "patterns": [
          {
            "group": [
              "http",
              "https",
              "http2",
              "net",
              "tls",
              "dgram",
              "dns",
              "node:http",
              "node:https",
              "node:http2",
              "node:net",
              "node:tls",
              "node:dgram",
              "node:dns"
            ],
            "message": "No codedocs package may reach the network (ADR 0011). scripts/no-network.ts is the same rule over the dependency closure."
          }
        ]
      }
    ],
    "no-restricted-globals": [
      "error",
      {
        "name": "fetch",
        "message": "No codedocs package may reach the network (ADR 0011). scripts/no-network.ts is the same rule over the dependency closure."
      },
      {
        "name": "XMLHttpRequest",
        "message": "No codedocs package may reach the network (ADR 0011). scripts/no-network.ts is the same rule over the dependency closure."
      },
      {
        "name": "WebSocket",
        "message": "No codedocs package may reach the network (ADR 0011). scripts/no-network.ts is the same rule over the dependency closure."
      },
      {
        "name": "EventSource",
        "message": "No codedocs package may reach the network (ADR 0011). scripts/no-network.ts is the same rule over the dependency closure."
      }
    ]
  },
  "overrides": [
    {
      "files": ["**/*.{js,ts,tsx}"],
      "env": {
        "es2020": true
      },
      "rules": {}
    }
  ]
}
